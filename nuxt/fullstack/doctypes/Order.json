{
	"name": "Order",
	"slug": "order",
	"fields": [
		{
			"fieldname": "id",
			"label": "ID",
			"component": "ATextInput",
			"fieldtype": "Data",
			"mode": "display"
		},
		{
			"fieldname": "orderNumber",
			"label": "Order Number",
			"component": "ATextInput",
			"fieldtype": "Data",
			"mode": "display"
		},
		{
			"fieldname": "userId",
			"label": "Customer",
			"component": "ACombobox",
			"fieldtype": "Link",
			"options": "user",
			"required": true
		},
		{
			"fieldname": "status",
			"label": "Status",
			"component": "ADropdown",
			"fieldtype": "Select",
			"options": ["DRAFT", "PENDING", "PROCESSING", "SHIPPED", "COMPLETED", "CANCELLED"],
			"default": "DRAFT"
		},
		{
			"fieldname": "total",
			"label": "Total",
			"component": "ATextInput",
			"fieldtype": "Currency",
			"mode": "display"
		},
		{
			"fieldname": "shippingAddress",
			"label": "Shipping Address",
			"component": "ATextarea",
			"fieldtype": "Text"
		},
		{
			"fieldname": "billingAddress",
			"label": "Billing Address",
			"component": "ATextarea",
			"fieldtype": "Text"
		},
		{
			"fieldname": "createdAt",
			"label": "Created At",
			"component": "ATextInput",
			"fieldtype": "Datetime"
		},
		{
			"fieldname": "updatedAt",
			"label": "Updated At",
			"component": "ATextInput",
			"fieldtype": "Datetime"
		}
	],
	"workflow": {
		"states": ["DRAFT", "PENDING", "PROCESSING", "SHIPPED", "COMPLETED", "CANCELLED"],
		"actions": {
			"save": {
				"label": "Save",
				"stateless": true
			},
			"submit": {
				"label": "Submit Order",
				"allowedStates": ["DRAFT"],
				"requiredFields": ["userId", "shippingAddress"],
				"nextState": "PENDING"
			},
			"process": {
				"label": "Process Order",
				"allowedStates": ["PENDING"],
				"nextState": "PROCESSING"
			},
			"ship": {
				"label": "Ship Order",
				"allowedStates": ["PROCESSING"],
				"nextState": "SHIPPED"
			},
			"complete": {
				"label": "Complete Order",
				"allowedStates": ["SHIPPED"],
				"nextState": "COMPLETED"
			},
			"cancel": {
				"label": "Cancel Order",
				"allowedStates": ["DRAFT", "PENDING", "PROCESSING"],
				"nextState": "CANCELLED"
			},
			"viewCustomer": {
				"label": "View Customer",
				"stateless": true,
				"clientHandler": "router.push(record.userId ? `/user/${record.userId}` : '/user')"
			}
		},
		"triggers": {
			"dateOrder": {
				"label": "Date order",
				"on": ["createdAt", "updatedAt"],
				"clientHandler": "if (record.createdAt && record.updatedAt && record.updatedAt < record.createdAt) setError('updatedAt', 'Updated date cannot be before created date')"
			}
		}
	}
}
