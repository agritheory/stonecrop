---
'@stonecrop/aform': patch
---

`ADateTime` saves the time its picker shows when only a date is picked on an empty field, where it also saved the clock's milliseconds, and its seconds when seconds were hidden.
