---
'@stonecrop/themes': minor
---

The floor adds primitive tokens (`--sc-color-*`, `--sc-space-*`, `--sc-gray-2`) that the existing names alias, plus surface tokens for the page, overlays, input addons and table cells.
