---
'@stonecrop/desktop': minor
---

SheetNav gains a `toolbar` slot, which Desktop forwards as `sheetnav-toolbar`, and exports `SHEET_NAV_TOOLBAR_SELECTOR` so controls anywhere on the page can teleport into it.
