---
'@stonecrop/schema': minor
'@stonecrop/graphql-middleware': minor
'@stonecrop/graphql-client': minor
---

A link field may declare `config` (a `TableViewConfig`), and the middleware serves it in field metadata to the client.
