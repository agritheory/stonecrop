---
'@stonecrop/themes': minor
---

Default colours are now gray: primary and brand are `--sc-gray-80`, borders, row lines and header text are darker, danger is `#c02718` and `--sc-font-size` is `1rem`; set `--sc-color-primary` and `--sc-color-brand` to keep a brand colour.
