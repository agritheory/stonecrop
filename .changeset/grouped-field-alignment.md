---
'@stonecrop/aform': patch
---

`ACurrencyInput` and `AQuantityInput` set their text at the size, padding and colour of every other field, and the currency prefix is as wide as the symbol or name it shows.
