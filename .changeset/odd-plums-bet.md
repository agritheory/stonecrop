---
'@stonecrop/desktop': minor
---

Desktop's ActionSet is now a tile column with a drawer: Search opens the command palette, `actionSetSlots` add host panels, the Actions tile lists the record's workflow actions (or `hostActions` when given), and `useActionSet().present()` opens a preview beside the record.
