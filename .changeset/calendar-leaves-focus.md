---
'@stonecrop/aform': patch
---

aform's calendar leaves focus where it was when it appears and moves to a day set from outside, so a day can be typed into `ADate` with the calendar open, and a form showing `ADuration` keeps its focus.
