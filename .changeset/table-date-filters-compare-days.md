---
'@stonecrop/atable': patch
---

The table's date filters compare the day each cell shows in the user's time zone, so a date-time range keeps every time on its last day and leaves out rows with no date.
