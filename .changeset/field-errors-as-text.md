---
'@stonecrop/aform': minor
---

Field errors render as text rather than HTML, and each input names its error through `aria-invalid` and `aria-describedby`; `ADropdown` is announced as a combobox with its options.
