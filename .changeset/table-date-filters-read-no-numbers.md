---
'@stonecrop/atable': patch
---

The table's date filters no longer move a date held as a number into the current year, so a number in a date or date-time column matches no day, as its default display of "Invalid Date" says.
