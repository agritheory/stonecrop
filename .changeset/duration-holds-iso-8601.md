---
'@stonecrop/aform': minor
---

`ADuration` holds an ISO 8601 duration such as `PT2H30M` instead of milliseconds, which an `interval` column stored as seconds, and shows the years, months and fractions of a second a stored duration holds.
