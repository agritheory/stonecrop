---
'@stonecrop/code-editor': minor
'@stonecrop/node-editor': minor
'@stonecrop/desktop': minor
---

Import the package's own stylesheet from its entry, so components are styled without a separate `./styles` import.
