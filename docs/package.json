{
	"name": "@stonecrop/docs",
	"version": "0.7.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build:site": "bash ../common/scripts/run-docs.sh --aggregate && vitepress build",
		"dev": "node --run build:site && vitepress dev",
		"preview": "node --run build:site && vitepress preview"
	},
	"devDependencies": {
		"mermaid": "^11.12.2",
		"search-insights": "2.17.3",
		"vitepress": "^1.6.4",
		"vitepress-plugin-mermaid": "^2.0.17"
	}
}
