{
	"name": "@stonecrop/docs",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vitepress dev",
		"build": "node scripts/aggregate-api.mjs && vitepress build",
		"build:full": "cd ../examples && rushx build:stories && cd ../docs && node scripts/aggregate-api.mjs && vitepress build",
		"preview": "vitepress preview"
	},
	"devDependencies": {
		"mermaid": "^11.4.0",
		"vitepress": "^1.5.0",
		"vitepress-plugin-mermaid": "^2.0.17"
	}
}
