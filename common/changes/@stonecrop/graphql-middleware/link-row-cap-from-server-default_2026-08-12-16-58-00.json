{
  "changes": [
    {
      "comment": "Many-side link rows are now capped by defaultRecordLimit instead of a hard-coded 50 that declaring the default fetch method silently removed, and truncation is reported in the new truncatedLinks field.",
      "type": "minor",
      "packageName": "@stonecrop/graphql-middleware"
    }
  ],
  "packageName": "@stonecrop/graphql-middleware"
}
