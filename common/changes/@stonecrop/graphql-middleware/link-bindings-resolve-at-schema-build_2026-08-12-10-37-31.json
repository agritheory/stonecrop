{
  "changes": [
    {
      "comment": "Schema build now refuses a link that binds to nothing — a many-side link with no `backlink`, or a one-side link whose `fieldname ?? key` names no declared field — and the one-side fetch path now honours that same key fallback.",
      "type": "minor",
      "packageName": "@stonecrop/graphql-middleware"
    }
  ],
  "packageName": "@stonecrop/graphql-middleware"
}
