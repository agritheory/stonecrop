{
  "changes": [
    {
      "packageName": "@stonecrop/graphql-middleware",
      "comment": "stonecropRecords now applies a default row cap of 200 when the caller names no limit, configurable via createStonecropPlugin({ defaultRecordLimit }), and reports the true total rather than the page size.",
      "type": "minor"
    }
  ],
  "packageName": "@stonecrop/graphql-middleware"
}
