{
  "changes": [
    {
      "packageName": "@stonecrop/graphql-middleware",
      "comment": "Schema build now refuses a doctype whose link or `inherits` names a doctype nothing registered, instead of silently dropping the relation at fetch time; the unused `validateReferences` export is removed.",
      "type": "minor"
    }
  ],
  "packageName": "@stonecrop/graphql-middleware"
}
