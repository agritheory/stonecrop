{
  "changes": [
    {
      "packageName": "@stonecrop/graphql-middleware",
      "comment": "stonecropAction now runs the guard read, the effect and the state write in one transaction with the guarded row locked FOR UPDATE, so a failed action rolls back its handler's writes instead of leaving them committed.",
      "type": "minor"
    }
  ],
  "packageName": "@stonecrop/graphql-middleware"
}
