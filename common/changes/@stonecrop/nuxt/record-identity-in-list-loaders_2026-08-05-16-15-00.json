{
  "changes": [
    {
      "comment": "The scaffolded page keys list rows by the identity the doctype declares rather than a hardcoded `id`, so a natural-keyed doctype no longer has every row silently dropped and its list render empty; the draft-id guard in its load handler is gone now that Desktop does not ask for a draft",
      "type": "patch",
      "packageName": "@stonecrop/nuxt"
    }
  ],
  "packageName": "@stonecrop/nuxt"
}
