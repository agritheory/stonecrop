{
	"changes": [
		{
			"packageName": "@stonecrop/nuxt",
			"comment": "type-check the fullstack example in test:types (add a fullstack tsconfig + wire it in), making the Desktop `@action=\"run\"` payload contract a compile-time regression guard; add the @stonecrop/graphql-client dependency it and the templates already import",
			"type": "patch"
		}
	],
	"packageName": "@stonecrop/nuxt"
}
