{
  "changes": [
    {
      "comment": "recordLookupField in the scaffolded resolvers now calls @stonecrop/schema's getRecordIdField rather than restating the rule. Behaviour is unchanged — these hosts already applied the id fallback; the Postgres adapter was the site that did not, which is what made the hosts disagree",
      "type": "patch",
      "packageName": "@stonecrop/nuxt"
    }
  ],
  "packageName": "@stonecrop/nuxt"
}
