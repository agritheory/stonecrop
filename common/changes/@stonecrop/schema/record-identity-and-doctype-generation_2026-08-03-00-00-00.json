{
  "changes": [
    {
      "packageName": "@stonecrop/schema",
      "comment": "Add getPrimaryKeyField/getRecordIdentity and INTROSPECTED_IDENTITY_PROPS; generation now verifies an existing doctype and stamps provenance instead of overwriting it, deriving primaryKey only for an unambiguous id. Replaces --overrides/typeOverrides with --names, and adds --check.",
      "type": "patch"
    }
  ],
  "packageName": "@stonecrop/schema"
}
