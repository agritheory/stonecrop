{
  "changes": [
    {
      "comment": "Return a page from DataClient.getRecords instead of a bare array: the result carries hasMore, so a truncated list is distinguishable from a complete one, plus a count that is opt-in behind the new includeTotal option.",
      "type": "minor",
      "packageName": "@stonecrop/schema"
    }
  ],
  "packageName": "@stonecrop/schema"
}
