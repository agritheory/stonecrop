{
	"changes": [
		{
			"packageName": "@stonecrop/desktop",
			"comment": "fix ActionSet `dropdownOpen` ref typing (boolean[] instead of the inferred never[]), which surfaced once the fullstack example began type-checking",
			"type": "patch"
		}
	],
	"packageName": "@stonecrop/desktop"
}
