{
  "changes": [
    {
      "comment": "Hold a new record's data in the component rather than HST, and route New Record to /{doctype}/new. Fixes silent data loss: a draft's HST node never existed, so every field write threw and the typed data survived only in a cached object any addRecord invalidated. A draft is now also seeded with the doctype's declared defaults.",
      "type": "minor",
      "packageName": "@stonecrop/desktop"
    }
  ],
  "packageName": "@stonecrop/desktop"
}
