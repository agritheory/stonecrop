{
  "changes": [
    {
      "comment": "Return the page from getRecords and record it per doctype, so getPageInfo can answer whether a list was truncated — a question HST cannot, since it holds only what was fetched.",
      "type": "minor",
      "packageName": "@stonecrop/stonecrop"
    }
  ],
  "packageName": "@stonecrop/stonecrop"
}
